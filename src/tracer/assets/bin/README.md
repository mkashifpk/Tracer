Place the Potrace executable here to enable the preferred tracing backend:

- `src/tracer/assets/bin/potrace.exe` on Windows

If `potrace.exe` is not present here, the app also checks the system `PATH`.

License note:
- Potrace is GPL-2.0 licensed. Review that license before bundling or distributing it with this application.
